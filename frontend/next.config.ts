import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['localhost', 'tokit-production.up.railway.app', 'via.placeholder.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
